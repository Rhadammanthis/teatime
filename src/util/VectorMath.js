/**
 * Calculates the vector between two points
 * @param {*} v1 The origin
 * @param {*} v2 The target
 */
export const pointSubstraction = (v1, v2) =>{

    return { x : v1.x - v2.x, y : v1.y  -v2.y }

}

